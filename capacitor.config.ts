import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.auth',
  appName: 'IonicAuth',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};
export default config;
